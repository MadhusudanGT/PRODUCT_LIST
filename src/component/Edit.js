import React from "react";
import Button from "@material-ui/core/Button";
const Edit = props => {
  return (
    <div>
      <Button variant="contained" color="default" disabled>Out OF Stock</Button>
    </div>
  );
};

export default Edit;

